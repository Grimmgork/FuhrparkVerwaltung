<Window x:Class="fvgui.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fvgui"
        mc:Ignorable="d"
        Title="Fahrzeugverwaltungssystem" Height="450" Width="800">

	<Window.DataContext>
		<local:MainViewModel/>
	</Window.DataContext>

    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="20"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<ListView Grid.Row="1" ItemsSource="{Binding Result}">
			<ListView.View>
				<GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Employee Information">
					<GridViewColumn DisplayMemberBinding="{Binding Path=InterneKennung}" Header="Interne Kennung" Width="100"/>
					<GridViewColumn DisplayMemberBinding="{Binding Path=Kennzeichnung}" Header="Kennzeichnung" Width="100"/>
					<GridViewColumn DisplayMemberBinding="{Binding Path=Verbrauch}" Header="Verbrauch" Width="100"/>
					<GridViewColumn DisplayMemberBinding="{Binding Path=Länge}" Header="Länge" Width="100"/>
					<GridViewColumn DisplayMemberBinding="{Binding Path=Breite}" Header="Breite" Width="100"/>
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</Window>
